import * as types from '../mutation-types'

// initial state
// shape: [{ id, quantity }]
const state = {
  added: []
}

// getters
const getters = {
}

// actions
const actions = {
}

// mutations
const mutations = {
  [types.ADD_PASSWORD] (state, { id }) {

  }
}

export default {
  state,
  getters,
  actions,
  mutations
}